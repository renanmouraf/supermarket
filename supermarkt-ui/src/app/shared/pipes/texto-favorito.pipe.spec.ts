import { TextoFavoritoPipe } from './texto-favorito.pipe';

describe('TextoFavoritoPipe', () => {
  it('create an instance', () => {
    const pipe = new TextoFavoritoPipe();
    expect(pipe).toBeTruthy();
  });
});
