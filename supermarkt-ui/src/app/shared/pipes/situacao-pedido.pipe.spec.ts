import { SituacaoPedidoPipe } from './situacao-pedido.pipe';

describe('SituacaoPedidoPipe', () => {
  it('create an instance', () => {
    const pipe = new SituacaoPedidoPipe();
    expect(pipe).toBeTruthy();
  });
});
