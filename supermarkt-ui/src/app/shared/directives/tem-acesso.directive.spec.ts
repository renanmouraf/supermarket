import { TemAcessoDirective } from './tem-acesso.directive';

describe('TemAcessoDirective', () => {
  it('should create an instance', () => {
    const directive = new TemAcessoDirective();
    expect(directive).toBeTruthy();
  });
});
