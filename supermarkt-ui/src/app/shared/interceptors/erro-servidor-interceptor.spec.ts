import { ErroServidorInterceptor } from './erro-servidor-interceptor';

describe('ErroServidorInterceptor', () => {
  it('should create an instance', () => {
    expect(new ErroServidorInterceptor()).toBeTruthy();
  });
});
